<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Biblioteca de Videos</title>

<!-- Fuente moderna -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

<style>
    :root {
        --bg: #0b0b0b;
        --bg2: #111;
        --panel: #141414;
        --accent: #4e9cff;
        --text: #e8e8e8;
        --text2: #b9b9b9;
        --radius: 18px;
        --shadow: 0 0 25px rgba(0,0,0,0.45);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        background: var(--bg);
        color: var(--text);
        font-family: "Inter", sans-serif;
        overflow-x: hidden;
    }

    /* NAV */
    nav {
        position: fixed;
        top: 0;
        width: 100%;
        padding: 20px 45px;
        background: rgba(10, 10, 10, 0.7);
        backdrop-filter: blur(10px);
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 1000;
        border-bottom: 1px solid #1d1d1d;
    }
    nav .logo {
        font-size: 26px;
        font-weight: 700;
        letter-spacing: -1px;
    }

    nav ul {
        display: flex;
        gap: 25px;
        list-style: none;
    }

    nav ul li a {
        text-decoration: none;
        color: var(--text2);
        font-size: 15px;
        transition: .3s;
    }
    nav ul li a:hover {
        color: var(--accent);
    }

    /* HERO */
    .hero {
        margin-top: 120px;
        width: 100%;
        padding: 120px 45px;
        text-align: center;
        background: linear-gradient(180deg, #131313, #0b0b0b);
    }

    .hero h1 {
        font-size: 52px;
        font-weight: 700;
        margin-bottom: 20px;
    }

    .hero p {
        font-size: 18px;
        color: var(--text2);
        max-width: 800px;
        margin: auto;
    }

    /* SECCIONES */
    .section-title {
        margin: 60px 45px 25px;
        font-size: 28px;
        font-weight: 700;
    }

    .contenedor {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        gap: 30px;
        padding: 0 45px 80px;
    }

    /* TARJETAS DE VIDEO */
    .video-card {
        background: var(--panel);
        padding: 18px;
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        transition: transform .25s, box-shadow .25s;
    }

    .video-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 0 35px rgba(0,0,0,0.6);
    }

    .video-card h2 {
        font-size: 20px;
        margin-bottom: 10px;
        font-weight: 600;
    }

    iframe {
        width: 100%;
        height: 230px;
        border-radius: var(--radius);
        border: none;
        margin-bottom: 12px;
    }

    .descripcion {
        color: var(--text2);
        font-size: 14px;
        margin-bottom: 8px;
    }

    .canal {
        font-size: 14px;
        color: var(--accent);
        font-weight: 600;
    }

    /* FOOTER */
    footer {
        padding: 25px;
        text-align: center;
        background: var(--bg2);
        border-top: 1px solid #1d1d1d;
        color: var(--text2);
        margin-top: 60px;
    }
</style>
</head>

<body>

<!-- NAV -->
<nav>
    <div class="logo">VideoHub</div>
    <ul>
        <li><a href="#nuevos">Nuevos</a></li>
        <li><a href="#coleccion">Colección</a></li>
        <li><a href="#favoritos">Favoritos</a></li>
    </ul>
</nav>

<!-- HERO -->
<section class="hero">
    <h1>Tu Biblioteca de Videos</h1>
    <p>
        Gestiona, visualiza y organiza tus videos desde una interfaz moderna, clara y elegante. 
        Simplemente añade tus enlaces dentro del código y la plataforma los mostrará en tarjetas visuales.
    </p>
</section>

<!-- SECCIÓN 1 -->
<h2 class="section-title" id="nuevos">Videos Nuevos</h2>
<div class="contenedor">

    <!-- VIDEO ITEM 1 -->
    <div class="video-card">
        <h2>Nombre del Video Aquí</h2>
        <iframe src="AQUI_TU_LINK" allowfullscreen></iframe>
        <p class="descripcion">Descripción del video aquí.</p>
        <p class="canal">Canal: Aquí</p>
    </div>

    <!-- VIDEO ITEM 2 -->
    <div class="video-card">
        <h2>Otro Video</h2>
        <iframe src="LINK_AQUI" allowfullscreen></iframe>
        <p class="descripcion">Descripción breve.</p>
        <p class="canal">Canal: Ejemplo</p>
    </div>

</div>


<!-- SECCIÓN 2 -->
<h2 class="section-title" id="coleccion">Colección Principal</h2>
<div class="contenedor">

    <!-- DUPLICA Y AGREGA VIDEOS -->
    <div class="video-card">
        <h2>Título</h2>
        <iframe src="LINK" allowfullscreen></iframe>
        <p class="descripcion">Texto descriptivo.</p>
        <p class="canal">Canal: Nombre</p>
    </div>

</div>

<!-- SECCIÓN 3 -->
<h2 class="section-title" id="favoritos">Favoritos</h2>
<div class="contenedor">

    <div class="video-card">
        <h2>Título favorito</h2>
        <iframe src="LINK" allowfullscreen></iframe>
        <p class="descripcion">Descripción del video.</p>
        <p class="canal">Canal: Ejemplo</p>
    </div>

</div>

<!-- FOOTER -->
<footer>
    Plataforma estética de videos — Diseño premium completo
</footer>

</body>
</html>
